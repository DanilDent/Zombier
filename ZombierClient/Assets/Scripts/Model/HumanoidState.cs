namespace Prototype.Model
{
    public enum HumanoidState
    {
        None,
        Idle,
        Chase,
        Fight,
        Hit,
        RestoringFromHit,
        PerformingAttack,
        Dead
    }
}
